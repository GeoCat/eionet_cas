version: '3'

services:

  apache:
    build: ./apache
    ports:
      - "80:80"
      - "443:443"

  cas:
    image: apereo/cas:v5.2.2
    ports:
      - "8443:443"

  geoserver:
    build: ./geoserver
    ports:
      - 8080:8080
